import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mimacom.devicecontrol',
  appName: 'Device Control',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
